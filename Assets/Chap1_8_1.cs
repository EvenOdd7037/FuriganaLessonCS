using UnityEngine;

public class Chap1_8_1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        string text = "ここはテキトーに何か書いていいらしい";
        Debug.Log(text);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
