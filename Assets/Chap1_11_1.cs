using UnityEngine;

public class Chap1_11_1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
      int seisuu = (int)23.6;
      string text =   "" + 110;
      Debug.Log(seisuu + text);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
