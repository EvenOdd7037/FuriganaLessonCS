using UnityEngine;

public class Chap1_8_2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        double kakaku = 150;
        double urine = kakaku * 1.27;
        Debug.Log(urine);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
